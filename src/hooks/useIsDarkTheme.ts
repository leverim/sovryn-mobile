// import { useMemo } from 'react';
// import { useColorScheme } from 'react-native';

// todo add automatic and forced schemes.
export function useIsDarkTheme(): boolean {
  // const scheme = useColorScheme();
  return true;
  // return useMemo(() => scheme === 'dark', [scheme]);
}
